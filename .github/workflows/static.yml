<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>CarPlay Style Dashboard • GitHub Pages</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      height: 100vh;
      font-family: -apple-system, BlinkMacC, "Segoe UI", Roboto, sans-serif;
      background: linear-gradient(135deg, #0f0c29, #302b63, #24243e);
      color: white;
      overflow: hidden;
    }

    .container {
      height: 100%;
      padding: 3vh 5vw;
      display: grid;
      grid-template-rows: auto 1fr auto;
      gap: 4vh;
    }

    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      font-size: 3.8vh;
      font-weight: 600;
      letter-spacing: -0.5px;
    }

    .temp {
      display: flex;
      align-items: center;
      gap: 0.8rem;
      font-size: 2.2vh;
      opacity: 0.9;
    }

    .apps-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 5vh 4vw;
      place-items: center;
    }

    .app {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 1.2vh;
      transition: transform 0.15s ease;
    }

    .app:hover {
      transform: scale(1.08);
    }

    .icon {
      width: 11vh;
      height: 11vh;
      background: rgba(255,255,255,0.08);
      border-radius: 28%;
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      border: 1px solid rgba(255,255,255,0.12);
      display: grid;
      place-items: center;
      font-size: 5.5vh;
      box-shadow: 0 8px 32px rgba(0,0,0,0.35);
    }

    .app-name {
      font-size: 1.9vh;
      font-weight: 500;
      opacity: 0.92;
      letter-spacing: 0.4px;
    }

    .bottom-bar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      font-size: 2.1vh;
      padding: 1.5vh 0;
      opacity: 0.9;
    }

    .now-playing {
      display: flex;
      align-items: center;
      gap: 1.2rem;
    }
  </style>
</head>
<body>

<div class="container">
  <!-- Top bar -->
  <div class="header">
    <div>07:42</div>
    <div class="temp">
      <span>☀</span> 18°
    </div>
  </div>

  <!-- Main apps grid -->
  <div class="apps-grid">
    <div class="app">
      <div class="icon">🗺️</div>
      <div class="app-name">Maps</div>
    </div>

    <div class="app">
      <div class="icon">🎵</div>
      <div class="app-name">Music</div>
    </div>

    <div class="app">
      <div class="icon">📞</div>
      <div class="app-name">Phone</div>
    </div>

    <div class="app">
      <div class="icon">💬</div>
      <div class="app-name">Messages</div>
    </div>

    <div class="app">
      <div class="icon">🎙️</div>
      <div class="app-name">Siri</div>
    </div>

    <div class="app">
      <div class="icon">🚗</div>
      <div class="app-name">Car</div>
    </div>
  </div>

  <!-- Bottom bar -->
  <div class="bottom-bar">
    <div class="now-playing">
      <span>♫</span>
      <span>The Weeknd — Blinding Lights</span>
    </div>
    <div>ETA • 14 min</div>
  </div>
</div>

<script>
  // Optional: live clock
  function updateClock() {
    const now = new Date();
    const h = String(now.getHours()).padStart(2,'0');
    const m = String(now.getMinutes()).padStart(2,'0');
    document.querySelector('.header > div:first-child').textContent = `${h}:${m}`;
  }
  setInterval(updateClock, 1000);
  updateClock();
</script>

</body>
</html>
